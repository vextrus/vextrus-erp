{
  "query": "mutation LoginUser($input: LoginInput!) { login(input: $input) { accessToken refreshToken user { id email username } } }",
  "variables": {
    "input": {
      "email": "test.integration@vextrus.com",
      "password": "TestPassword123!"
    }
  }
}
