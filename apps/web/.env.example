# Vextrus ERP Web Frontend - Environment Variables Example
# Copy this file to .env.local for local development or .env.production for production

# ==============================================
# API Configuration (REQUIRED)
# ==============================================
# GraphQL API Gateway URL
# Local development: http://localhost:4000/graphql
# Docker Compose: http://api.localhost/graphql
# Production: https://api.yourdomain.com/graphql
NEXT_PUBLIC_API_URL=http://localhost:4000/graphql

# WebSocket URL for GraphQL subscriptions
# Local development: ws://localhost:4000/graphql
# Docker Compose: ws://api.localhost/graphql
# Production: wss://api.yourdomain.com/graphql
NEXT_PUBLIC_WS_URL=ws://localhost:4000/graphql

# ==============================================
# Application Configuration
# ==============================================
# Node environment (development | production | test)
NODE_ENV=development

# Next.js telemetry (1 = disabled, 0 = enabled)
NEXT_TELEMETRY_DISABLED=1

# ==============================================
# Feature Flags
# ==============================================
# Enable bundle analyzer (true | false)
ANALYZE=false

# Development mode features (true | false)
NEXT_PUBLIC_DEV_MODE=false

# Debug logging (true | false)
NEXT_PUBLIC_DEBUG=false

# ==============================================
# Security Configuration
# ==============================================
# JWT token expiration in seconds (default: 3600 = 1 hour)
TOKEN_EXPIRY=3600

# ==============================================
# Internationalization
# ==============================================
# Default locale (en | bn)
NEXT_PUBLIC_DEFAULT_LOCALE=en

# Available locales (comma-separated)
NEXT_PUBLIC_LOCALES=en,bn

# ==============================================
# Monitoring & Observability (Optional)
# ==============================================
# Sentry DSN for error tracking
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Google Analytics ID
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# ==============================================
# Notes
# ==============================================
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - Server-side variables (without NEXT_PUBLIC_) are only available during build/server runtime
# - Never commit secrets or sensitive data to .env files
# - Use Docker secrets or environment-specific secret management for production
