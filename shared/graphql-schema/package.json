{
  "name": "@vextrus-erp/graphql-schema",
  "version": "1.0.0",
  "description": "Shared GraphQL schema types for Vextrus ERP Federation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm run build"
  },
  "keywords": [
    "graphql",
    "schema",
    "federation",
    "shared",
    "pagination"
  ],
  "author": "Vextrus ERP Team",
  "license": "PRIVATE",
  "dependencies": {
    "@nestjs/graphql": "^12.0.0",
    "graphql": "^16.8.1"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  },
  "publishConfig": {
    "registry": "http://localhost:4873/"
  }
}
